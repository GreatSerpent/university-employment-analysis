# Employment vs International Students Analysis

This project analyzes the relationship between the employment rate and the proportion of international students using Python.

## How to run
1. Install dependencies: `pip install -r requirements.txt`
2. Ensure `data.csv` is in the project folder.
3. Run the script: `python main.py`
